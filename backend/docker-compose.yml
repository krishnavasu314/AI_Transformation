version: "3.9"

services:
  mysql:
    image: mysql:8.4
    container_name: ai-task-mysql
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: task_knowledge_db
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
